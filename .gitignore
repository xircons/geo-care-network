# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist.old.*
dist-ssr
*.local

# Vite writes timestamped temp configs on Windows-style fs when it can't
# delete the previous one (our case via the read-only mount).
vite.config.*.timestamp-*

# Original Vite scaffold assets that we don't reference anymore.
src/assets/

# Local env (committed: .env.example only)
.env
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
